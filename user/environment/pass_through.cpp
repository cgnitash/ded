


/*
 * only needed for core build system 
 * everything is provided trivially by header
 *
#include "pass_through.h"

#include <algorithm>
#include <fstream>
#include <iostream>
#include <numeric>
#include <random>
#include <regex>
#include <string>
#include <string_view>
#include <utility>
#include <vector>

life::population
    pass_through::evaluate(life::population pop)
{

  // make and configure nested environment
  auto env = life::make_environment(env_);

  return env.evaluate(pop);
}
*/
